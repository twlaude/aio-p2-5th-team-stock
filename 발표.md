# 분산 실행과 발표 환경 정리

## 1. 발표에서 설명할 핵심

우리 프로젝트는 하나의 컴퓨터에 모든 코드가 묶인 프로그램이 아니다. Frontend, Backend, MCP Client, 네 MCP 서버, Database와 Redis를 독립 실행 단위로 분리한다.

각 폴더는 최종적으로 자기 `Dockerfile`, `.env.example`, `requirements.txt`, 실행 설명서를 갖는다. 따라서 Backend 폴더만 받은 팀원도 Backend 컨테이너를 실행할 수 있다.

다만 Backend만으로 전체 종목 분석이 완성되는 것은 아니다. Backend의 `.env`에 다른 컴퓨터에서 실행 중인 PostgreSQL, Redis, MCP Client 주소가 설정되어 있어야 한다. 다른 서버가 없을 때는 Mock 모드로 단독 개발한다.

## 2. 전체 연결 구조

```text
[Frontend :8501]
        ↓ HTTP
[Backend :8000]
   ├─ PostgreSQL :5432
   ├─ Redis :6379
   └─ [MCP Client :8010]
            ├─ [Price MCP :8020]
            ├─ [News MCP :8021]
            ├─ [Disclosure MCP :8022]
            └─ [Community MCP :8023]
```

MCP 서버를 다른 컴퓨터에서 호출하려면 `stdio`가 아니라 Streamable HTTP처럼 네트워크 접근이 가능한 전송 방식을 사용한다. 각 서버는 외부 접근이 가능하도록 `0.0.0.0`에 바인딩하고, Windows 방화벽에는 필요한 포트만 허용한다.

## 3. 개발 중 권장 컴퓨터 배치

팀원 4명의 컴퓨터를 다음처럼 나누는 방안을 제안한다. 역할 확정 전의 배치 예시이며, 실제 담당자는 팀 상황에 맞게 바꿀 수 있다.

| 컴퓨터 | 실행 서비스 | 주요 역할 |
|---|---|---|
| 오현님 PC | Frontend, Backend, Redis | 화면, 로그인, Memory, 개인화, 전체 확인 |
| 인혜님 PC | MCP Client, Price MCP, News MCP | Agent Workflow, 네 MCP 통합, 주가·뉴스 |
| 기화님 PC | Disclosure MCP, PostgreSQL, pgvector | DART, 기업보고서 RAG, 영구 DB |
| 태웅님 PC | Community MCP, 커뮤니티 데이터 서버 | 커뮤니티 원본 연결과 반응 분석 |

## 4. 컴퓨터별 환경변수 연결 예시

### 오현님 PC Backend

```text
MCP_CLIENT_URL=http://인혜님_PC_IP:8010
DATABASE_URL=postgresql://계정:비밀번호@기화님_PC_IP:5432/stock_db
REDIS_URL=redis://redis:6379
```

### 인혜님 PC MCP Client

```text
PRICE_MCP_URL=http://price-mcp:8020/mcp
NEWS_MCP_URL=http://news-mcp:8021/mcp
DISCLOSURE_MCP_URL=http://기화님_PC_IP:8022/mcp
COMMUNITY_MCP_URL=http://태웅님_PC_IP:8023/mcp
```

### 기화님 PC Disclosure MCP

```text
DART_API_KEY=개인_환경변수에_설정
DATABASE_URL=postgresql://계정:비밀번호@postgres:5432/stock_db
```

### 태웅님 PC Community MCP

```text
COMMUNITY_API_URL=http://커뮤니티_원본_서버
COMMUNITY_API_TOKEN=개인_환경변수에_설정
```

## 5. 서비스별 비밀정보 분리

| 서비스 | 보관할 정보 |
|---|---|
| Frontend | Backend URL, 비밀정보 없음 |
| Backend | 인증 비밀키, 개인화 LLM Key, DB·Redis 주소 |
| MCP Client | 공통 분석용 LLM Key, 네 MCP URL |
| Price MCP | 공공데이터포털 Service Key |
| News MCP | 뉴스 API Key |
| Disclosure MCP | DART·임베딩 API Key, DB 주소 |
| Community MCP | 커뮤니티 서버 Token |

한 팀원이 모든 API Key를 가질 필요가 없다. 담당 서비스에 필요한 키만 관리한다.

## 6. 개발 환경과 발표 환경

### 개발

```text
팀원 PC 4대에 서비스 분산
```

- 각자 맡은 서버만 설치하고 실행할 수 있다.
- 다른 서버는 IP와 포트로 호출한다.
- 필요한 서버가 없으면 계약과 같은 Mock 응답으로 개발한다.

### 통합 테스트

```text
4대 연결 → Frontend부터 네 MCP 서버까지 전체 왕복 확인
```

- 각 서비스에 `/health` 또는 연결 상태 확인 기능을 둔다.
- IP, 포트, 방화벽, 환경변수를 먼저 점검한다.
- 일부 MCP가 실패해도 전체가 멈추지 않고 부분 성공을 표시한다.

### 발표

```text
발표용 노트북 1대에서 전체 Docker Compose 실행
```

발표 중에는 Wi-Fi, 팀원 PC 절전, IP 변경 문제를 줄이기 위해 한 대에서 전체 서비스를 실행하는 구성을 준비한다. 실제 외부 API가 실패할 경우를 대비해 삼성전자 Mock Data 모드도 함께 준비한다.

## 7. 발표 데모 흐름

1. 데모 사용자 A로 로그인한다.
2. 삼성전자를 검색한다.
3. Backend가 MCP Client에 분석을 요청한다.
4. MCP Client의 Workflow와 Agent가 MCP Tool을 사용한다.
5. 뉴스·공시·커뮤니티 근거가 통합된다.
6. 공통 한줄평, 시장 관심 온도, 근거 확인 정도가 표시된다.
7. 사용자 A의 투자 성향에 맞는 확인 포인트를 보여준다.
8. 사용자 B로 바꿔 공통 분석은 같고 개인화만 달라지는 것을 보여준다.

## 8. 발표에서 강조할 문장

> 우리는 주가를 예측하거나 종목을 추천하지 않습니다. 서로 흩어진 뉴스, 공식 공시, 실제 커뮤니티 반응을 연결하고, 사용자가 자신의 투자 관점에서 무엇을 먼저 확인해야 하는지 설명합니다.
