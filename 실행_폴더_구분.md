# 실제 구동 폴더와 참고 폴더 구분

이 문서는 팀원이 어떤 폴더를 개발해야 하고 어떤 폴더는 실행하지 않아도 되는지 구분하기 위한 안내서다.

## 1. 실제로 실행되는 서비스 폴더

아래 일곱 서비스 폴더는 개발이 끝나면 각각 독립 프로세스 또는 Docker 컨테이너로 실행된다.

| 폴더 | 실행 여부 | 역할 |
|---|---|---|
| `frontend/` | 실행 필수 | 사용자 화면, Backend API 호출 |
| `backend/` | 실행 필수 | 로그인, 투자 성향, Memory, 개인화 |
| `mcp_client/` | 실행 필수 | 네 MCP를 관리·호출·취합하는 통합 서버 |
| `mcp_servers/price_mcp/` | 실행 필수 | 주가·등락 정보 Tool 서버 |
| `mcp_servers/news_mcp/` | 실행 필수 | 실시간 뉴스 Tool 서버 |
| `mcp_servers/disclosure_mcp/` | 실행 필수 | DART·사업보고서 RAG Tool 서버 |
| `mcp_servers/community_mcp/` | 실행 필수 | 커뮤니티 반응 Tool 서버 |

각 실행 서비스에는 최종적으로 `requirements.txt`, `.env.example`, `Dockerfile`, 실행 진입 파일과 자체 테스트가 필요하다.

### 현재 실제 실행 가능 상태

| 폴더 | 현재 상태 |
|---|---|
| `frontend/` | 최소 Streamlit 화면만 실행 가능, 실제 Backend 분석 연결은 없음 |
| `backend/` | FastAPI `/health`만 실행 가능, 인증·분석·Memory API는 없음 |
| `mcp_client/` | 문서와 자리표시 폴더만 있음, 아직 실행 불가 |
| `mcp_servers/price_mcp/` | 문서와 자리표시 폴더만 있음, 아직 실행 불가 |
| `mcp_servers/news_mcp/` | 문서와 자리표시 폴더만 있음, 아직 실행 불가 |
| `mcp_servers/disclosure_mcp/` | 문서와 자리표시 폴더만 있음, 아직 실행 불가 |
| `mcp_servers/community_mcp/` | 문서와 자리표시 폴더만 있음, 아직 실행 불가 |
| `infra/` | 가이드만 있고 Docker Compose는 아직 없음 |

즉, 현재 폴더는 팀원이 개발을 시작할 수 있도록 책임과 자리를 나눈 상태이지 전체 서비스가 구동되는 완성본이 아니다.

현재 `backend/.env.example`과 `backend/app/core/config.py`는 이전 단일 MCP 골격의 설정 이름이 남아 있다. 개발 담당자는 이를 최종 계약으로 사용하지 말고 `backend/GUIDE.md`와 `shared/CONNECTION_CONTRACT.md`에 맞춰 교체해야 한다.

## 2. 실행을 지원하지만 자체 서버는 아닌 폴더

| 폴더 | 직접 실행 | 역할 |
|---|---|---|
| `db/` | 실행하지 않음 | PostgreSQL Schema, Migration, Seed 데이터 |
| `infra/` | 설정 실행 | PostgreSQL·Redis와 전체 Docker Compose 설정 |
| `shared/` | 실행하지 않음 | 팀 전체가 지켜야 하는 API·Tool 계약 |
| `tests/` | 테스트할 때만 | 서비스 사이의 계약·통합·발표 시나리오 테스트 |

`db/`는 Database 서버 자체가 아니다. PostgreSQL과 Redis 컨테이너는 `infra/` 설정으로 실행하고 `db/`의 파일을 적용한다.

`shared/`에는 실행 코드를 두지 않는다. 각 팀원은 개발 전에 `shared/CONNECTION_CONTRACT.md`와 자신이 담당한 계약 문서를 읽어야 한다.

## 3. 실행하지 않는 참고·문서 폴더

| 폴더 또는 파일 | 용도 |
|---|---|
| `guide/` | 초기 아이디어와 상세 설계 참고 문서 |
| `legacy/` | 이전 단일 MCP 골격 보존, 현재 실행 금지 |
| `text/` | 이미지 생성 프롬프트와 결과 자료 |
| `FE_화면_설계.md` | 화면 이해와 구현 참고 |
| `발표.md` | 분산 실행과 발표 준비 참고 |
| `plan.md` | 개발 순서와 범위 참고 |
| `README.md` | 프로젝트 입구 안내 |

`legacy/` 코드는 참고만 하고 현재 서버로 실행하거나 새 기능을 추가하지 않는다. 재사용할 부분이 있으면 담당 서비스 폴더로 옮겨서 사용한다.

## 4. 서비스 폴더 안에서의 구분

예를 들어 Backend 담당자는 다음처럼 이해하면 된다.

```text
backend/
├─ app/              실제 실행 코드
├─ tests/            Backend 개발 테스트
├─ requirements.txt  Backend 설치 패키지
├─ .env.example      필요한 환경변수 이름
├─ Dockerfile        Backend 컨테이너 실행 설정
└─ *.md              역할과 계약 설명, 직접 실행하지 않음
```

현재 여러 하위 폴더의 `README.md`는 코드가 아니라 “여기에 무엇을 구현할지” 알려주는 자리표시 문서다. 담당자는 해당 폴더에 Python 코드를 추가하되 문서에 적힌 책임을 바꾸지 않는다.

## 5. 담당자별 최소 확인 범위

| 담당 영역 | 개발할 폴더 | 반드시 함께 읽을 폴더 |
|---|---|---|
| Frontend | `frontend/` | `shared/contracts/frontend_backend/` |
| Backend | `backend/` | `shared/contracts/frontend_backend/`, `shared/contracts/analysis/`, `shared/contracts/user_profile/` |
| MCP Client | `mcp_client/` | `shared/contracts/analysis/`, `shared/contracts/price/`, `news/`, `disclosure/`, `community/` |
| Price MCP | `mcp_servers/price_mcp/` | `shared/contracts/price/` |
| News MCP | `mcp_servers/news_mcp/` | `shared/contracts/news/` |
| Disclosure MCP | `mcp_servers/disclosure_mcp/`, `db/` 일부 | `shared/contracts/disclosure/` |
| Community MCP | `mcp_servers/community_mcp/` | `shared/contracts/community/` |

## 6. MCP Client에 넣지 않는 기능

MCP Client는 관리자이자 연결 통로다. 다음 기능을 직접 가지지 않는다.

- 공공데이터·NAVER·DART·커뮤니티 원본 API 직접 호출
- 회원가입·로그인·투자 성향·사용자 Memory
- 주가·뉴스·공시·커뮤니티 원본 저장
- Frontend 화면과 관리자 페이지

MCP Client에 필요한 기능은 네 MCP Tool 발견·호출, Workflow, Agent Runtime, 결과 취합, 공통 분석 생성, 출처와 오류 정리뿐이다.
